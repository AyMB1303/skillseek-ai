/**
 * Obsolète — les notifications sont désormais persistées en base et servies
 * par l'API (`/api/notifications`), avec un destinataire précis par rôle.
 * Ce fichier est conservé vide pour éviter tout import résiduel.
 *
 * Voir : backend/app/services/notifications.py et components/Layout.jsx
 */
export {};
